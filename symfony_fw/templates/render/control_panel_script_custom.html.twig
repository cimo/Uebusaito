<script nonce="{{php_session.xssProtectionValue}}">
    "use strict";
    
    $(document).ready(() => {
        //...
        
        $(window).on("resize", "", (event) => {
            //...
        });
        
        $(window).on("orientationchange", "", (event) => {
            //...
        });
    });
    
    window.textCpCustom = {
        'label_1': "textCpCustom"
    };
</script>